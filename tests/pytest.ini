[pytest]
# Used when pytest is invoked from inside tests/ (rootdir = tests/). For a bare `pytest`
# run at the repo root, the root conftest.py provides the same sys.path + marker setup.
pythonpath = ..
markers =
    slow: LP-solve-heavy tests (~1.4 s per solve); deselect with -m "not slow"
